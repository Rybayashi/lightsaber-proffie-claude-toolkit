# Normalise line endings so a contributor on Linux or macOS does not see
# "the whole file changed" on a repository authored on Windows.
* text=auto

# PowerShell 5.1 is happiest with CRLF.
*.ps1 text eol=crlf

# Keep shell scripts LF even when checked out on Windows.
*.sh text eol=lf
